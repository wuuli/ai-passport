<p align="right">
  <strong>简体中文</strong> · <a href="PRD_TIME_DUEL.md">English</a>
</p>

# Time Duel（掐秒对战）— 产品需求文档

## 当前范围

两名玩家共用**同一台 AI Passport**，估算相同的目标时长。绝对误差较小者赢下回合，先赢三回合者赢得整场比赛。

当前评审产物为[独立交互原型](../prototype/time-duel-v2.html)。它在浏览器中运行，尚未实现固件或设备通信。此前的双机 BLE 设想及对应实现票已被本范围取代。

## 已确认需求

- 双人比赛在同一台设备上进行，无需 NFC 或 BLE 配对。
- 目标从 `{1.0, 1.5, 2.0, 2.5, 3.0}` 秒中抽取，步进 0.5 秒。
- 每名玩家通过设备按键控制自己估时的开始与结束。
- 计时过程中不显示已过时间。
- 五局三胜，平局不占五个计分回合。
- 单回合获胜和整场胜利使用不同的庆祝动画。

## 原型交互假设

当前原型采用**轮流传递设备，共用 OK 键**。该操作方式及以下细节属于待评审建议，并非额外的用户已确认需求。

板载上、下、OK 三个按键共用一个 ADC 分压电路，BSP 按互斥电压窗口识别按键，不能假定同时按两键能产生独立事件。轮流操作适配现有输入接口。

### 单回合流程

1. 公布目标与先手玩家，先手按 OK 开始。
2. 显示当前玩家和中性吉祥物，再按 OK 锁定成绩。
3. 暂不公开成绩，提示将 Passport 交给下一位；下一位准备好后按 OK。
4. 再次显示相同目标，下一位按 OK 开始，再按 OK 停止。
5. 按 `abs(估时 - 目标)` 比较双方锁定的成绩。
6. 播放约 1.5 秒的回合庆祝，点名赢家，再显示双方估时、误差及新比分。OK 可跳过动画。
7. 按 OK 继续。每次挑战交换先手，包括平局重赛；平局保留目标、比分和回合编号。
8. 一方先赢三局后，先展示最后一局的庆祝与成绩，再进入整场胜利界面。OK 开始新比赛，长按 OK 返回入口。

### 计时与公平性

- 双方都结束前不公开任何一方的估时，包括设备交接期间。
- 估时期间没有倒计时、已过时间、进度条或可作为节拍器的周期动画。
- 未停止的挑战在 `目标 + 3 秒` 自动结束，以该时长作为成绩，并在结算中标明自动停止。
- 忽略 200 ms 防抖窗口内的重复按键。
- 固件应在 `BSP_BTN_PRESS` 时用 `esp_timer_get_time()` 记录时间戳，再通知游戏逻辑；回调中不进行音频或显示工作。
- 浏览器原型使用 `performance.now()`，估时取整到毫秒；浏览器结果不能证明真机计时精度。
- 传递设备和轮换先手可以减轻顺序影响，但不能阻止玩家观察对方的按键动作。

## 视觉与音效

复用 `main/ui_pixel.c` 的像素天空、草地、黑边面板和小电视吉祥物。右上角保留不遮挡装饰的电量标识；浏览器里的设备外壳与电量读数为示意。

- **回合获胜：**共享屏幕点名赢家，吉祥物弹跳、ROUND WIN 横幅出现、比分跳动；平局使用中性的 DRAW 状态。
- **整场胜利：**点名冠军，展示奖杯和彩带、最终比分，以及再战或退出入口。
- **内存：**ESP32-C3 无 PSRAM，动画使用 `lv_anim` 变换已有对象，不使用全屏 RGB565 逐帧动画。
- **音效：**开始、停止、胜、负四种短提示音，由 worker task 播放；无背景音乐。原型提供可选的合成试听音效。

## 单人练习

同一台设备支持 AI 对手。玩家完成挑战后，AI 独立生成估时，再统一结算。建议使用高斯误差模型，`sigma = k * 目标`，原型暂用 `k = 0.12`。较小的 `k` 表示更精准、更强的对手，难度档位待试玩校准。

## 固件实现方向

- `main/duel_clock.c/h`：独立于硬件的计时状态机、交接、判定、比分与 AI 模型。
- `main/duel_ui.c`：共享屏幕的 LVGL 展示及两层庆祝动画。
- `main/demo_duel.c`：页面生命周期、按键事件、音频分发与演示页注册。
- `tests/test_duel_clock.c`：覆盖状态迁移、平局、超时、轮换先手、提前结束比赛与带种子的 AI 行为。
- 不需要游戏专用 BLE 协议、配对任务或 central 角色配置变更。

保留 3 MB 应用限制、受保护的 cardid/Recovery 分区、现有长按返回约定和上键五秒 Recovery 入口；删除界面前停止页面所属任务、回调和定时器。

## 验收目标

以下是实现要求，不是已完成的验证结果。

- 纯游戏模型和带种子的 AI 行为通过 host 测试。
- 完整固件门禁通过，分区和 Recovery 兼容性保持不变。
- 在一台真机上完成双人比赛，包含交接时隐藏成绩、平局、忘记停止时自动结束、两层庆祝、再战及长按退出。
- 在实际设备上检查按键时序、屏幕可读性、音效及内存占用。

## 待评审事项

1. 确认建议的轮流操作方式和交接时隐藏成绩。
2. 真机试玩后调整目标范围及 AI 难度。
3. 在 240 × 320 屏幕上调整动画姿态、提示时长与信息密度。
